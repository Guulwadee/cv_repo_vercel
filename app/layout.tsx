export const metadata = {
  title: 'CV Builder',
  description: 'Bilingual (EN/SO) resume builder with beautiful templates'
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
